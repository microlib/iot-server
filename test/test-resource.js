const assert = require('assert');

describe('Resource', () => {
  describe('Serve', () => {
    it('should return a resource given its ID', () => {
      assert(true);
    });
    it('should return all the resources', () => {
      assert(true);
    });
  });
  describe('Store', () => {
    it('should save a resource given its type', () => {
      assert(true);
    });
  });
});
